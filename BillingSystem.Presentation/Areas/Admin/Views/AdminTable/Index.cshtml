@model IEnumerable<BillingSystem.EntityLayer.Concrete.Table>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
<div class="container">
    <h1 class="text-center">Tables</h1>
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-3 mt-3">
                <div class="card">
                    <div class="card-body">
                        <h2 class="text-center">@item.TableName</h2>
                        <h5 class="card-title text-center mb-4">
                            @if (item.IsOccupied.Equals(true))
                            {
                                <span class="badge text-bg-success">Free</span>
                            }
                            else
                            {
                                <span class="badge text-bg-danger">Busy</span>
                            }
                        </h5>
                        <div class="text-center fs-5">
                            <a asp-area="Admin" asp-controller="AdminOrder" asp-action="Create" asp-route-id="@item.TableId" class="btn btn-primary w-100">Create order</a>
                            <a href="#" class="btn btn-danger w-100 mt-2">Billing</a>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

